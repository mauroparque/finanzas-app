rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for now (single-family app, no auth)
    // You can add more restrictive rules later if needed
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
